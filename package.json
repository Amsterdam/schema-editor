{
  "name": "schema-editor",
  "version": "0.1.0",
  "private": true,
  "homepage" : "https://amsterdam.github.io/schema-editor",
  "dependencies": {
    "@datapunt/asc-ui": "^0.17.7",
    "ajv": "^6.10.2",
    "axios": "^0.19.0",
    "immutable": "^4.0.0-rc.12",
    "lodash": "^4.17.15",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "3.2.0",
    "react-syntax-highlighter": "^11.0.2",
    "use-undo": "^1.0.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "yarn run build; push-dir --dir=build --branch=gh-pages --cleanup"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
